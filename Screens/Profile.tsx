import React from 'react';
import {Text, View} from 'react-native';
import {theme} from '../Constants/index';

const Profile = () => (
  <View>
    <Text style={{color: theme.colors.blue}}>Profile</Text>
  </View>
);

export default Profile;
