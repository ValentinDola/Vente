import React from 'react';
import {Text, View} from 'react-native';
import {theme} from '../Constants/index';

const Carte = () => (
  <View>
    <Text style={{color: theme.colors.blue}}>Carte</Text>
  </View>
);

export default Carte;
